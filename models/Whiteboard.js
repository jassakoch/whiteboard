// models/Whiteboard.js

class Whiteboard {
    constructor(id, title, createdBy) {
        this.id = id;
        this.title = title;
        this.createdBy = createdBy;
    }
}

export default Whiteboard;

